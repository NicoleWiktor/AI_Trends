"""
TODO: Build a LangChain Runnable/chain for classification

This chain should:
- Take title + summary + content
- Prompt LLM to output lane (technical/general) + confidence + rationale
- Parse and normalize the output
"""

# TODO: Import langchain core (ChatPromptTemplate, RunnableSequence, OutputParsers)
# TODO: Define the prompt template
# TODO: Build the chain with chosen LLM (OpenAI/Anthropic) based on settings
# TODO: Add a parser to extract fields (lane, confidence, rationale)
# TODO: Expose function `build_classification_chain()`
